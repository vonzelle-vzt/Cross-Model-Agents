version = "2.0.0"
model = "gpt-5.5"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = "Audit for secrets exposure, unsafe command patterns, and policy regressions. Recommend concrete mitigations."
